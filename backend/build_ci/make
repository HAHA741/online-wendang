#!/usr/bin/bash

# Set npm registry for faster installation
npm config set registry https://registry.npmmirror.com/

# Install production dependencies only
npm install --omit=dev

echo "Build completed successfully"
